# ── Frontend (Vite) ──────────────────────────────────────────────────────────
# URL del backend en Railway (sin barra final, sin /api)
# Dejarlo vacío en desarrollo local si usás npm run dev (detecta localhost:3001 automáticamente)
VITE_API_URL=https://tu-backend.up.railway.app
